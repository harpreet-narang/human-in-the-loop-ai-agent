# Example only. Do not commit real credentials.

OPENAI_API_KEY=your_key

# The workflow itself uses n8n credential storage for the connected chat model.
# No production API or database credential is required for the safe demo executor.
